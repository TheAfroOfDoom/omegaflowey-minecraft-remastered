# Teleport root player to marker's position + rotation
$execute as $(root_player_uuid) run teleport @s ~ ~ ~ ~ ~

function entity:hostile/omega-flowey/attack/homing-vines-save-states/player/marker/terminate
